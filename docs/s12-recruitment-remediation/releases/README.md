# Release Evidence

Store immutable deployment identifiers, commit SHAs, migration identifiers, validation output summaries, timestamps, environment names, rollback notes, and post-deploy smoke-test results here. Never store credentials, tokens, personal data, or raw production exports.

